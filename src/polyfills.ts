if (typeof Set === 'undefined') {
  console.warn('Set is not supported in this browser. Some features may not work correctly.');
}

if (typeof Promise === 'undefined') {
  console.warn('Promise is not supported in this browser. Some features may not work correctly.');
}

export {};
